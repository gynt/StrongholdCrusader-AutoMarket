name: automarket
display-name: Auto Market
version: 0.0.1
author: Sh0wd0wn
type: module
dependencies:
  framework: ">=3.0.5"
  frontend: ">=1.0.10"
  # Required because otherwise the overlay menu
  # doesn't show as the game is rendered over it
  graphicsApiReplacer: ">=1.2.1"
